// don't define a variable more than once.

let petName = 'Perrin';
 petName = 'Spot';

console.log(petName);

// there are rules related to variable names

// no numbers first
// no reserved keywords can be the name of a variable.

